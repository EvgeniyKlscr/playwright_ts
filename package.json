{
  "name": "playwright_udemy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tests:e2e": "playwright test --config=e2e.config.ts",
    "tests:api": "playwright test --config=api.config.ts",
    "test-local": "yarn lint && npx playwright test --headed",
    "test-ci": "npx playwright install && npx playwright test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.32.3",
    "prettier": "^2.8.8"
  }
}
